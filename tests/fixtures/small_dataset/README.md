# Small Fixture Dataset

This fixture has six tiny PPM images and six LabelMe-style JSON annotation files.

It is intentionally small so tests can validate partition and crop behavior without touching the real research dataset.
